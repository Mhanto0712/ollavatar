{
  "scripts": {
    "total_dev": "concurrently -c green,blue \"npm run back_dev \" \"npm run front_dev\"",
    "back_dev": "cd backend/api && fastapi dev main.py",
    "front_dev": "cd frontend && npm run dev",
    "total_start": "concurrently -c green,blue \"npm run back_start\" \"npm run front_start\"",
    "back_start": "cd backend/api && uvicorn main:app --host 0.0.0.0 --port 8000",
    "front_start": "cd frontend && npm run build && npm run start"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
